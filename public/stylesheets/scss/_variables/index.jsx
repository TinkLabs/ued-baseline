import React from 'react';

export default function Variables(props) {
    return (
        <span className="subtitle2">
            This SCSS file contains most of the variables that is used within the library, including a color blending function that combines a foreground and a background color.
        </span>
    )
}
