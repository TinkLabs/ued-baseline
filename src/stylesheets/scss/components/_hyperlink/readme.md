### Hyperlink

All default styling are removed for styling other components, so use the class name `hyperlink` if you need the most basic link behavior.

### Usage

```html
<a href="/" class="hyperlink body2"> link text </a> 
```
